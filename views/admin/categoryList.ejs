        <%- include('../adminLayout/adminHeader.ejs') %>

            <section class="content-main">
                <div class="content-header">
                    <h2 class="content-title">Category List</h2>
                    <div>
                        <a href="/admin/category/add-category" class="btn btn-primary"><i class="material-icons md-plus"></i> Create new</a>
                    </div>
                </div>
                <div class="card mb-4">
                   
                    <!-- card-header end// -->
                    <div class="card-body">
                        <!-- categoryList.ejs -->
<div class="row">
  <% categories.forEach(category => { %>
    <% if (!category.isDeleted) { %>
      <div class="col-xl-3 col-lg-4 col-md-6">
        <div class="card card-product-grid">
          <a href="#" class="img-wrap">
            <img src="/category/<%= category.categoryImage %>" alt="<%= category.categoryName %> Image" />
          </a>
          <div class="info-wrap">
            <div class="dropdown float-end">
              <a href="/admin/category/edit-cat?id=<%= category._id %>" class="btn btn-sm btn-brand rounded">
                <i class="material-icons md-edit mr-5"></i>Edit
              </a>
              <a  class="btn btn-danger text" href="/admin/category/cat-delete?id=<%= category._id %>" onclick="confirmDelete('<%= category._id %>', event)" >Unlist</a>
            </div>
            <a href="#" class="title"><%= category.categoryName %></a>
          </div>
        </div>
      </div>
    <% } %>
  <% }); %>
</div>
                          
                          
  
                           
                            
                        </div>
                        <!-- row.// -->
                    </div>
                    <!-- card-body end// -->
                </div>
                <!-- card end// -->
                <div class="pagination-area mt-15 mb-50">
                  <nav aria-label="Page navigation example">
                    <ul class="pagination justify-content-start">
                      <% for (let i = 1; i <= totalPages; i++) { %>
                        <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                          <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                        </li>
                      <% } %>
                    </ul>
                  </nav>
                  
                </div>
            </section>
            <!-- content-main end// -->
           
        
         <script>
            function confirmDelete(categoryId,event){
                var confirmation= confirm('Are you sure want to delete')
                if(!confirmation){
                    event.preventDefault();
                    console.log('Deletion canceled')
                }
            }
         </script>
          
          <%- include('../adminLayout/adminFooter.ejs') %>